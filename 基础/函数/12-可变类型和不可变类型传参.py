"""
在函数调用过程中，进行参数的传递
如果是不可变对象，在函数体内的修改不会影响实参的值
如果是可变对象，在函数体内的修改会影响到实参的值
"""


def test(a):
    a = 100


def demo(nums):
    nums[0] = 10


x = 1
test(x)
print(x)  # 1

y = [3, 4, 5, 1, 6]
demo(y)
print(y)  # [10, 4, 5, 1, 6]
